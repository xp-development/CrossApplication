<UserControl x:Class="CrossApplication.Mail.Wpf.Navigation.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:navigation="clr-namespace:CrossApplication.Mail.Wpf.Navigation"
             xmlns:viewModels="clr-namespace:CrossApplication.Wpf.Common.ViewModels;assembly=CrossApplication.Wpf.Common"
             xmlns:ca="urn:cross-application"
             mc:Ignorable="d"
             viewModels:ViewModelLocator.AutoWireViewModel="{x:Type navigation:NavigationViewModel}"
             d:DataContext="{d:DesignInstance navigation:NavigationViewModel}">
    <TreeView ItemsSource="{Binding NavigationItems}">
        <TreeView.ItemTemplate>
            <DataTemplate>
                <ca:TextControl Text="{Binding Label}" />
            </DataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>
</UserControl>